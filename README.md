# Analytics Lab 1 - Ava Lawless

## Recipe for Opening a New Working Environment

**1. Fork a repository in GitHub and open a GitHub CodeSpace**

**2. Open this CodeSpace in VSCode by clicking "New Window" > "Connect to..." > Click your CodeSpace's name**

**3. Create an interactive .py file by using # %% to make your code able to run**

**4. Open a bash terminal by clicking "Terminal" > "New Terminal"**

**5. Create a virtual environment by typing source .venv/bin/activate**

**6. Load the requirements.txt file in the terminal by typing "pip install -r requirements.txt"**

**7. Install plotly in your terminal by typing "pip install plotly"**

**8. Add plotly to your requirements from the terminal by typing "echo "plotly==23.2.1" >> requirements.txt"**

**9. Commit your changes to GitHub with these steps:**

git add .

git commit -m "message about your changes"

git push origin main
